class DecidingService:
    def clean_up_service(self):
        del name
        del reason
        options_dict.clear()
        options_list.clear()
        variables_dict.clear()
        variables_list.clear()
        result.clear()